'use strict';

function f1(str) {
	return str.toUpperCase();
}
/*
let Logging = require("./js-logging.js");
let logger = new Logging({
	filters : [
	           f1, 'blue', //default filter
	           //the last item can be custom filter. here is "warning" and "error" filter
	           {
	        	   warning : 'yellow',
	        	   error : [f1, 'red' ]
	           }
	]
});
logger.setLevel(0);
logger.debug('1');
logger.warning('2');
*/

let logger1 = require("./js-logging.js").colorConsole();
logger1.error('asdfasdf');

let logger2 = require("./js-logging.js").console({
	filters : [
	           f1, 'green'
	]});
logger2.setLevel('error'); 
logger2.debug('asdfasdf');
logger2.error('asdfaskjfhldsakf');

logger1.debug('logger1');

	let logger3 = require('./js-logging.js').console({
		transport : function(data) {
			console.log(data.output);
			return data;
		}
	});
let o = logger3.info('hello');
console.log(o);


let logger4 = require("./js-logging.js").dailyFile({
	filenameDateFormat: 'yyyymmdd HHMM'
});
logger4.error('asdfasdf');

